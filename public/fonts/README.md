# Hero font — Plantin Infant

The hero uses **Plantin Infant MT Std Regular** from:

`PlantinInfant-Regular.otf`

Source archive: `plantin-infant-mt-std-regular_KznYS.zip` (extracted to `plantin-infant/`).

For production, commit `PlantinInfant-Regular.otf` to the repo so Vercel can serve it.
